#include "main.h"

/**
 * add - adds two ints passed into it
 * @n: first number passed into func
 * @m: second number passed into func
 * Return: i (value of two numbers added)
 */

int add(int n, int m)
{
	int i;

	i = n + m;

	return (i);
}
