#include "main.h"
#include <string.h>

/**
 * _strlen - returns the string length
 * @s : the string input
 * Return: c (value of string length)
 */

int _strlen(char *s)
{
	int c;

	c = strlen(s);

	return (0);
}
