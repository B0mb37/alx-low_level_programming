#include "main.h"
#include <string.h>

/**
 * _strcmp - compares two str
 * @s1: first input
 * @s2: second input
 * Return: answer
 */

int _strcmp(char *s1, char *s2)
{
	int answer;

	answer = strcmp(s1, s2);

	return (answer);
}
