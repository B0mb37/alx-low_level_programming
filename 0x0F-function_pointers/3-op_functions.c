#include "3-calc.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * op_add - funct thatadds two values, returns sum
 * @a: first number
 * @b: second number
 * Return: c (value after operation)
 */

int op_add(int a, int b)
{
	int c = a + b;

	return (c);
}

/**
 * op_sub - funct that subtracts two values
 * @a: first number
 * @b: second number
 * Return: c (value after operation)
 */

int op_sub(int a, int b)
{
	int c = a - b;

	return (c);
}

/**
 * op_mul - funct that multipliess two values
 * @a: first number
 * @b: second number
 * Return: c (value after operation)
 */
int op_mul(int a, int b)
{
	int c = a * b;

	return (c);
}
/**
 * op_div - funct that divides two values
 * @a: first number
 * @b: second number
 * Return: c (value after operation)
 */

int op_div(int a, int b)
{
	int c = a * b;

	return (c);
}

/**
 * op_mod - funct that gets mod of two values
 * @a: first number
 * @b: second number
 * Return: c (value after operation)
 */
int op_mod(int a, int b)
{
	int c = a % b;

	return (c);
