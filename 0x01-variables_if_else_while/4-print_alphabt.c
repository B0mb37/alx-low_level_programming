#include <stdio.h>

/**
 * main - prints lowercase alphabet except q and e
 * Return: 0 (success)
 */


int main(void)
{
	char x = 'a';

	while (x <= 'z')
	{
		if (x != 'e' && x != 'q')
		{
			putchar(c);
		}
		x++;
	}
	putchar("\n");
	return (0)
}
